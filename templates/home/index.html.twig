{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!

{% endblock %}

{% block body %}
	{% if app.user %}
		<section>
			{% for genre in genres %}
				<div class="title">
					<h2>{{ genre.name }}</h2>
				</div>
				<div class="carouselSlider">
					<div class="carouselContainer">
						<ul>
							{% for video in genre.videos %}
								<a href="">
									<li>
										<img src="{{asset('uploads/thumbnail/' ~ video.thumbnail)}}" alt="">
										<p>{{video.title}}</p>
										<p>{{video.category}}</p>
									</li>
								</a>
							{% endfor %}
						</ul>
					</div>
				</div>
			{% endfor %}
		</section>
	{% else %}
		{% include "components/_homeNotLogged.html.twig" %}
	{% endif %}
{% endblock %}
